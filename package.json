{
  "name": "pincer-protocol",
  "version": "0.1.0",
  "description": "Pincer Protocol - Agent Economy Payment Infrastructure",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:baseSepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "verify": "hardhat verify --network baseSepolia",
    "clean": "hardhat clean",
    "node": "hardhat node"
  },
  "keywords": [
    "pincer",
    "pncr",
    "blockchain",
    "ethereum",
    "base",
    "agent-economy",
    "defi"
  ],
  "author": "Pincer Protocol",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  }
}
